{
  "name": "pixel-forge",
  "version": "1.0.0",
  "private": true,
  "description": "Local-first media conversion and optimization for images and video.",
  "scripts": {
    "build:styles": "tailwindcss -i ./frontend/tailwind-input.css -o ./static/styles/app.css --minify",
    "sync:frontend-assets": "node ./scripts/sync-fontawesome-assets.mjs",
    "build:frontend": "npm run sync:frontend-assets && npm run build:styles"
  },
  "license": "MIT",
  "devDependencies": {
    "@fortawesome/fontawesome-free": "^6.7.2",
    "tailwindcss": "^3.4.17"
  }
}
